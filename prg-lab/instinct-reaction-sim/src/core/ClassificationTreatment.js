(function (global) {
  const MPER = global.MPER || (global.MPER = {});

  class ClassificationTreatment {
    static process(data) {
      return data;
    }
  }

  MPER.ClassificationTreatment = ClassificationTreatment;
})(window);
